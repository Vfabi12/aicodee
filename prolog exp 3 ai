CODE 1

% Facts
male(john).
male(james).
female(mary).
female(linda).
parent(john, mary).
parent(john, james).
parent(linda, mary).
parent(linda, james).

% Rules
father(X, Y):- male(X), parent(X, Y).
mother(X, Y):- female(X), parent(X, Y).

CODE 2

EVEN ODD
even(X) :- 0 is X mod 2.

odd(X) :- 1 is X mod 2.

CODE 3


dog(rottweiler).
cat(munchkin).
animal(A) :- dog(A).
